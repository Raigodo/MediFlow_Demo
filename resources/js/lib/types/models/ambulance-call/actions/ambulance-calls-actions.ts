import { ResourceActions } from '../../reource-actions';
import { ResourceActionsItem } from '../../reource-actions-item';

export interface AmbulanceCallsActions extends ResourceActions {
    create: ResourceActionsItem;
}
